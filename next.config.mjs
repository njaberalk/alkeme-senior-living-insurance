/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/senior-living',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

export default nextConfig
